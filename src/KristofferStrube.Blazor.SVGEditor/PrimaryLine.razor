<line x1=@SVG.LocalTransform(From).x.AsString() y1=@SVG.LocalTransform(From).y.AsString() x2=@SVG.LocalTransform(To).x.AsString() y2=@SVG.LocalTransform(To).y.AsString() style="pointer-events: none;" stroke-dasharray="4" stroke="black" stroke-width="2.5" />
<line x1=@SVG.LocalTransform(From).x.AsString() y1=@SVG.LocalTransform(From).y.AsString() x2=@SVG.LocalTransform(To).x.AsString() y2=@SVG.LocalTransform(To).y.AsString() style="pointer-events: none;" stroke-dasharray="4" stroke="white" stroke-width="0.5" />

@code {
    [Parameter]
    public (double x, double y) From { get; set; }

    [Parameter]
    public (double x, double y) To { get; set; }

    [CascadingParameter]
    protected SVG SVG { get; set; }
}
